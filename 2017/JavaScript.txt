1. read csv file

	i: module:read-line (async)

lineReader.eachLine('集奥接口.csv', function(line, last){
	console.log(line);
})

	ii: module: csv (async)

var stream = fs.createReadStream("集奥接口.csv");
 
csv
 .fromStream(stream)
 .on("data", function(data){
     console.log(data);
     console.log(data.length);
 })
 .on("end", function(){
     console.log("done");
 });

	iii: module: read-line-sync (sync)

lineReader.eachLine('集奥接口.csv', function(line, last){
	console.log(line);
})

LineReaderSync = require("line-reader-sync")
lrs = new LineReaderSync("集奥接口.csv")
while(true){
  var line = lrs.readline()
  if(line === null){
    console.log("EOF");
    break;
  }else{
    console.log("line without \n",line)
  }
}
//or 
console.log(lrs.toLines())

2. Module

